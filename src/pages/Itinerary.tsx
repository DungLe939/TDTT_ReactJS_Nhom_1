import SchedulePage from '../modules/schedule/pages/SchedulePage/SchedulePage';

/**
 * Component Itinerary - Trang Lịch trình
 * 
 * Render trực tiếp Component `SchedulePage` từ Module Schedule.
 * Đảm bảo toàn bộ logic giao tiếp với Backend và UI phức tạp được giữ nguyên.
 */
export const Itinerary = () => {
  return <SchedulePage />;
};

export default Itinerary;